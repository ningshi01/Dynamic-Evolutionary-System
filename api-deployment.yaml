apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
  namespace: rag
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: crater-harbor.act.buaa.edu.cn/user-liujh24/minio-api-gateway:latest # <-- IMPORTANT: Replace with your image
        ports:
        - containerPort: 8000
        env:
        - name: FILE_SERVICE_URL
          value: "http://fileservice-service"
        - name: PARSER_SERVICE_URL
          value: "http://parser-service"
---
apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: rag
spec:
  selector:
    app: api-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: NodePort 